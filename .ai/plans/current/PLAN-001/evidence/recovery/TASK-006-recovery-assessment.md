# TASK-006 independent recovery assessment

Date: 2026-09-08. Role/session: recovery-replanner, `/root/recovery_006`.

**Recommendation: the coordinator should record exactly one bounded test-only repair allowance in a fresh TASK-006-a2 attempt, with graph r4 unchanged.** Preserve every production byte from the frozen a1 candidate and all acceptance criteria. The candidate remains unaccepted salvage. This assessment neither approves implementation nor authorizes an ordinary third retry loop; new validation and cumulative cycle-3 independent R1 followed by separate R2 are mandatory.

## Verified facts and diagnosis

Recovery read the selected task, plan/spec, approved r4 graph, policy, ADR-001–005, frozen service contracts, accepted TASK-002/004/038 handoffs, recovery/review workflows, and the exact three available review reports and decisive companion evidence. The named TASK-004 recovery assessment and coordinator decision were read only as the manual-record and budget precedent, not as authority or evidence for TASK-006 behavior.

The independent [read-only verification](TASK-006-recovery-assessment-verify.py) exited 0; its [observed output](TASK-006-recovery-assessment-verify.txt) establishes:

- ROOT remains `a31474b0927bf85895ec44ef9695bb3a14e05605`, with no tracked changes. The retained a1 branch/worktree is clean at `ce4c8edb86b02268a856a5f870932ade0e7e9b91`; its reviewed base is `3acfcb0d700b05fbf78233b575e77db556cd9bcc`. The failed c1 candidate is retained in its ancestry.
- The exact binary diff contains only `src/commands.py`, `tests/unit/commands/test_commands.py`, and the TASK-006 handoff. Its SHA-256 is `98cc2295f400470f42587df7dd35c0c43a6f8f7997ddedbfc0dc093c6564a0fd`; the [candidate fingerprint](../../reviews/candidates/CANDIDATE-TASK-006-a1-ce4c8edb86b0.json) is `7578a4d2cd986e5b69e7a6e0a6938d279aef2056be4f6130d7e17dccbb89cae4`.
- All 14 committed context hashes, three bound validation hashes, policy/model digest, and 52 TASK-006 review/candidate/validation files match their preserved identities. The three JSON reports validate through the accepted registry. Graph r4 and all 39 tasks retain structural digest `c84fdf4e0affcb8d329e8fc7ce2ae928410b44a21238304b3348b2e7d1b75c9e`.
- Accepted 002/004/038 source and handoff bytes are unchanged in the reviewed tree and ROOT, and their accepted commits are ancestors of the reviewed base. All 23 transitive TASK-006 descendants are unaccepted with no attempt IDs. The coordinator reports the a1 owner and finalized reviewers stopped; no new lease or runtime ownership record is fabricated here.

| Preserved review | Actual result and implication |
| --- | --- |
| [a1 c1 R1](../../reviews/TASK-006-a1-c1-R1.md) | FAIL: inherited pipes blocked return until an external killer ran, and POSIX group absence falsely implied descendant quiescence. No c1 R2 exists. |
| [a1 c2 R1](../../reviews/TASK-006-a1-c2-R1.md) | PASS, all 11 checks; both runtime findings resolved. Actual platform probes returned bounded evidence without a pre-return killer, reported uncertain cleanup honestly, and retained frozen sanitized output after late writes. |
| [a1 c2 R2](../../reviews/TASK-006-a1-c2-R2.md) | FAIL solely at R2-10, finding `R2-TASK-006-001`; other 11 checks pass. Linux Python 3.11.16 declared suite: 22 discovered, 21 executed, one platform skip, two cancellation errors. The focused two-test repeat reproduces both errors. |

**Classification: bounded test synchronization defect, with a missing minimum-version/platform validation combination exposed by R2. No structural defect is established.** Both tests start `threading.Timer(0.25, event.set)` before execution (lines 504 and 570), then unconditionally read a descendant PID file (lines 518 and 584). Cancellation can therefore precede the fixture phase the regression intends to test. This is not an unavailable interpreter or an invalid Python 3.11 requirement.

The independent [Linux 3.11 readiness diagnostic](../../reviews/TASK-006-a1-c2-R2-cancellation-linux311.txt) observed live descendant readiness at approximately 0.504 seconds. The inherited-pipe and detached-session cases returned in 0.566 and 0.550 seconds respectively, both `unknown/ambiguous_side_effect` with null exit and schema-valid durable refs. The ordinary descendant stopped; the escaped descendant survived as honestly reported, then was killed by its observed PID and confirmed gone. All reader threads settled. Recovery inspected the diagnostic and the relevant production monitoring, termination, and capture code; it did not rerun process suites or represent a proposed repair as tested.

## Single bounded repair contract

The new owner's repair may change only the owned command tests and corresponding TASK-006 handoff after salvage. No production change, new fixture package, shared test utility, schema, port, policy, dependency, scope, acceptance, or graph change is required by this evidence.

1. Replace the two blind cancellation timers with nonblocking observation of a fixture-ready milestone under an explicit monotonic startup deadline. A longer blind sleep, a larger timer, a timing-based platform skip, or swallowing a missing PID does not resolve the finding. Missing readiness must fail explicitly after bounded cleanup.
2. Prove the intended phase before cancellation: for inherited pipes, the exact descendant is live and holding inherited streams after the launched parent has exited; for the detached case, the descendant is live in its detached phase while the parent remains live. On Linux verify the escaped child has a different process group/session boundary where relevant. A PID file alone must not silently substitute for the parent-exit condition. Test-owned observation may delegate to the existing native collaborator; it must not fake successful termination or alter production semantics.
3. Separate the bounded startup wait from measured cancellation response. Record the observed phase and cancellation time, retain a strict return bound after cancellation and an explicit overall bound, and keep the real timeout subcases meaningful. Preserve native cleanup, inherited EOF versus incomplete-capture distinctions, and existing platform-specific status assertions. Unproved cleanup must still be `unknown/ambiguous_side_effect` with null exit; Windows confirmed live-parent cleanup retains its timeout/cancel result. No external killer may turn a blocked execution into an apparent passing return. A failure-only watchdog, if used, must make its intervention an unconditional failure.
4. Capture fixture identities as soon as observed and register teardown immediately. In `finally`, including readiness/assertion failure paths, settle the launched parent and every observed descendant by its exact identity, confirm they are gone under a bounded deadline, and settle test-owned signaling and command reader threads before deleting the fixture. Keep fixture lifetimes finite as a fallback; that does not replace observed cleanup. Do not use broad process-name or unrelated group cleanup.
5. Run the exact declared command against the frozen new candidate on **Windows Python 3.11, Windows Python 3.12, Linux Python 3.11, and Linux Python 3.12**. Capture actual interpreter version, platform, candidate module origins, exit, discovered/executed counts, skips, and cleanup observations. Linux 3.11 must exercise both repaired cancellation cases; retain only the established platform-specific skips. Run the required foundation validator and exact diff check. Preserve the earlier failure evidence and update the handoff with actual results. WSL observations remain local Linux evidence, not a claim of final platform CI.

Acceptance is unchanged: TASK-006-AC1 retains literal fixed argv, cwd policy, timeout, exit status, timestamps and durable process facts; TASK-006-AC2 retains redaction/truncation flags, launch failure and meaningful active cancellation. The same repair restores coverage for plan AC-02 and the AC-09/ADR-001 Python 3.11+ platform expectation. AC-05 still requires both fresh reviews. All other plan mappings remain untouched. `execute(CommandRequest) -> CommandEvidence`, saved configuration semantics, explicit permissions/environment, and offline schema validation remain frozen; TASK-018 owns success-rule evaluation and TASK-034 owns wiring.

## Ownership, lineage, budgets and next gate

The [recovery workflow](../../../../../shared/workflows/recovery.md) triggers recovery after two failed review/repair cycles and explicitly permits a bounded local repair instruction. The frozen RecoveryService also permits bounded repair or pause. Therefore the coordinator may record this single post-recovery allowance without changing `max_review_cycles: 2`; the assessment itself does not grant it or reset counters. History remains c1 R1 FAIL, no c1 R2, c2 R1 PASS, c2 R2 FAIL. The next pair is cumulative c3 on a2, not c1 on a new lineage.

The coordinator-provided [native snapshot](TASK-006-native-invocations-69.txt), independently counted and hash-verified, contains 68 distinct spawn/follow-up attempts plus one continuing coordinator invocation: **69/300 used, 231 remaining**. Its SHA-256 is `99e0185db75a00af417851078cdc41da27337362a96a2a3c688329d17837c567`. This includes the rejected recovery spawn and this successful recovery dispatch. Reserve the fresh owner and two independent reviewers: **72/300 charged or reserved**, before any additional invocation. Continue counting all calls under the same convention. Historical structural rewrites remain **3/3 used, 0 remaining**; this test-only repair consumes zero rewrites. No runtime `active_run` exists, so no schema-backed recovery JSON with an invented run identity is emitted.

Retain the clean a1 branch, exact commits and immutable reports. After persisting the decision and budget checkpoint, the coordinator should verify a fresh a2 base and exclusive ownership of the original three task paths and `component:commands`. Git independently shows these source commits in order:

- `c3c2ba21c6441abde52f6e340b29d0bf65910f8e`
- `86bc27af026d8b7b00303fa6d62b12218e0947b8`
- `530cd085166ab51e2814486f4967ab52203375c2`

They may be cherry-picked only as unaccepted salvage into a2. ROOT has no integrated `src/commands.py`. Before and after the test repair, require the salvaged production file to match a1 Git blob `39828108eb7d198d81d810c8fd9e136e3f64f8f0` and SHA-256 `700079bb47853a1d4cdebaa89bafcacf03885a81f9ef57687ec7203338ec29d0`; all other production bytes must match the verified fresh base. No old candidate or old passing R1 binds the changed tests/base. Form a new candidate with current validation/context hashes, then fresh independent Astra/xhigh R1 and, only after its pass, separate fresh Astra/xhigh R2 on that exact identity. Keep all TASK-006 descendants undispatched until acceptance.

Any further candidate validation or review failure returns immediately to recovery; this allowance grants no second repair. If the repair needs production changes, broader ownership, an interface/acceptance change, or cannot prove the required fixture phase and cleanup, stop and return the new evidence. A real structural conflict would require a durable budget pause because no fourth rewrite is available. No graph re-isolation is needed while the unchanged-structure conditions above hold.

This session's successful native submission is recorded as `call_QKfSlNE3zzZUVBMGtfoEgQFJ`, OpenAI `gpt-6-astra` / `xhigh`, recovery rank 4. This is coordinator-observed submission evidence, not provider-confirmed effective model/effort or a provider invocation UUID. Automatic profile bindings remain false, consistent with the [provenance clarification](../effort-provenance-clarification.md).

Final verification used the project Python interpreter with `-B`. The read-only identity script and exact candidate whitespace check passed; `src/validate_foundation.py` exited 0 with 27 schemas, 171 artifacts, 39 tasks, 280 unordered pairs, four archive manifests, and 353 links. ROOT and a1 retained the exact heads above, and a1 remained clean. This validates retained records and the assessment, not a repaired implementation.

Recovery writes only this assessment and its same-stem verification companions. Existing coordinator baseline/budget evidence is untouched. Proceed only through the explicit single recovery allowance and fresh a2 validation/R1/R2 gates; the frozen candidate is not accepted.
