# TASK-004 independent recovery assessment

Date: 2026-09-08. Role/session: recovery-replanner, `/root/recovery_004`.

**Recommendation: record an explicit recovery decision permitting one bounded task-local repair, with no graph rewrite, subject to the acceptance and budget conditions below.** Do not accept the frozen candidate. This is a repair recommendation, not implementation approval or permission for an ordinary third retry loop. The demonstrated failure is an overbroad recognizer inside TASK-004's owned digest implementation; the current evidence does not require new product scope, a new public signature, or different dependencies. Arbitrary path-versus-text intent in opaque strings is a separate representation limit and must not be hidden by another heuristic.

## Evidence and actual state

- At initial inspection, integration branch `ai/PLAN-001/integration` was at `77502377774bd5294ae0857035d40fe1698742c0`. The coordinator subsequently preserved the finalized cycle-2 failure at observed root head `dd4312b1e526e7be9e666e20719d6846f66f05c5`; only this advisory remained untracked. This advisory is the only file written by recovery.
- Frozen branch `ai/PLAN-001/TASK-004/a1`, worktree `.worktrees/TASK-004-a1`, remained clean at `248f4dedf5d37d09eb27a3ef08bb63a570359666`. Recovery independently verified its binary diff hash and candidate fingerprint `900aecdaa2a27efbe2bc869df51a5f07f846d492b0523c77ab5673848ef72fec` against [the candidate](../../reviews/candidates/CANDIDATE-TASK-004-a1-248f4dedf5d3.json).
- The base-to-head diff contains exactly `src/contracts.py`, `src/validate_foundation.py`, `src/install.py`, `tests/unit/schemas/test_contracts.py`, and the task's implementation handoff. The first repair changed only the digest module, owned tests, and handoff. No schema, shared contract, policy, graph, CLI, or other task implementation changed.
- Graph `PLAN-001-r4` is approved. Both bootstrap and candidate digest functions independently returned `c84fdf4e0affcb8d329e8fc7ce2ae928410b44a21238304b3348b2e7d1b75c9e`. Only TASK-001 is accepted. TASK-003's separate worktree was observed at `c2de9b7a8e613690677c876d5843a620ee405b16`; it supplies no accepted dependency here.

| Available history | Observed outcome |
| --- | --- |
| [Cycle 1 R1](../../reviews/TASK-004-a1-c1-R1.md), head `7d92dda5237b57087a4c7e869deb72c9028ab82e` | Fail: objective-prose collision and task-bucket directory relocation mismatch. AC1/AC3 fail; AC2 passes. |
| Repair commit `6206a07700210e6dd138747c2a96fa36a2100240` | Field-aware recognition and task directory handling; candidate-bound evidence reports 17 focused tests passing. Handoff separately records 24 bootstrap tests and foundation validation passing. |
| [Cycle 2 R1](../../reviews/TASK-004-a1-c2-R1.md), frozen head above | Final fail, `R1-TASK-004-003`: input-contract prose retains approved graph identity. Both exact earlier reproductions are fixed. AC1/AC3 still fail; AC2 passes. |
| R2 history | No TASK-004 R2 report exists for either cycle; neither candidate reached that gate. Absence is not a pass. |

The finalized [cycle-2 evidence](../../reviews/TASK-004-a1-c2-R1-evidence.txt) establishes approved-graph impact, all 27 v1 kinds, 54 provider/separator/plan/task-bucket relocation combinations, real scope invalidation, and source-isolated installed dependency closure. Recovery read this evidence and independently executed read-only Python probes against the frozen module, with the root `.ai/local/full-plan-venv/Scripts/python.exe`; exit 0, with these observed results:

| Mixed-field value, changed only from `current` to `completed` | Schema-valid | ScopePath-valid | Candidate digests equal |
| --- | --- | --- | --- |
| `input_contracts`: `.codex/plans/current/PLAN-101/spec.json: required shape comes from docs/contract.md` | yes | no | **yes: defect** |
| `input_contracts`: `.codex/plans/current/PLAN-101/spec.json requires review.md` | yes | yes | **yes: further prose collision** |
| `output_contracts`: `.codex/plans/current/PLAN-101/spec.json` | yes | yes | **no: pure reference relocation gap** |

Thus rejecting colons, requiring an extension, or merely calling `ScopePath` is insufficient. Recovery did not rerun the broad suites and does not claim that a proposed repair passed them. Failed exploratory inspection commands were corrected; they supplied no validation conclusion. No source edit, commit, canonical transition, or cleanup occurred.

## Bounded repair contract

Use a deterministic, field-aware projection with a **closed whole-value language for known references**, not natural-language or filename plausibility detection:

1. Preserve the bootstrap structural field list, task ordering, compact sorted-key JSON, UTF-8, and `ensure_ascii=False`. Preserve separators, provider namespace, case, all non-lifecycle components, list order, and exact current-location bytes. Canonicalize only the known plan/task bucket components to `current`; never add a second acceptable stale hash.
2. In explicit reference fields (`spec_refs`, `adr_refs`, `research_refs`) and explicit scope path fields, use their declared path/reference semantics. Recognize rooted provider/plan/task lifecycle positions component-wise, including exact directory-prefix claims. Do not apply location substitution to resources, objectives, acceptance text, titles, rationale, handoff text, or other prose.
3. In **both** mixed contract arrays, recognize only a complete documented managed-record form. The necessary closed core consists of `.ai|.codex|.claude`, `plans`, a lifecycle bucket, a valid `PLAN-NNN`, then an exact fixed plan record (`plan.json`, `plan.md`, `spec.json`, `spec.md`, `graph.json`), or `tasks/<bucket>/TASK-NNN.json`, or the corresponding known plan/task directory with its trailing separator. Other managed forms may be included only with an equally explicit complete layout grammar and supporting contract evidence. Parse the entire value; a suffix following a known record, arbitrary free-form tail, embedded path, URL, or unrelated root must remain byte-exact text. Do not promote a generic extension-bearing filename into a known reference.
4. Preserve existing plan-qualified logical references unchanged. Prefer those already-supported identities for references outside the known physical language. Do not silently migrate existing task strings, rewrite historical snapshots, depend on current filesystem existence, or change the digest when the same immutable task data is read on another host.

This interpretation follows r4 AC3's explicit restriction to **known** plan/task locations. It is not a claim that a string-only schema can infer all authors' intentions. A sentence and a valid filename can have identical bytes. If a required lifecycle case involves an arbitrary physical filename indistinguishable from prose and cannot be represented by the existing known-location/logical-reference contracts, **stop this repair as a structural discovery**. Do not silently shrink the lifecycle requirement, declare every ambiguous string a path, or claim that a whitespace blacklist solves the general problem. The implementer must show that the closed language covers every required existing relocation case; failure of that condition is not grounds to waive AC3.

Required evidence before the next candidate:

- Preserve all three TASK-004 acceptance criteria. AC1 retains all-kind offline validation, strict versions/fields, qualified references, and the common digest. AC2 retains typed errors and constructor parity. AC3 retains the complete installed helper closure, exact bootstrap identity, lifecycle equality, and structural invalidation.
- Keep the 54-combination relocation coverage and add paired pure-reference/prose cases in both contract arrays, including the two independently reproduced sentences, trailing punctuation/separators, non-ASCII text, and text ending in an extension. Test known task files and bucket directories, arbitrary non-lifecycle changes, and logical-reference identity changes. Prove that an approved graph rejects structural mutations; equality-only digest tests are insufficient.
- Show unchanged fixed plan/task physical references relocate, while changed suffix text and changed real structure invalidate approval. Every unmatched mixed-field string must be retained exactly; whole-value matching must not strip or rewrite text.
- Re-run the owned schema suite, existing 24-test bootstrap suite, foundation validator, and standalone installed-tool isolation with the accepted TASK-001 dependency only. Update the handoff with the exact recognition boundary and actual results. Retain both failed reports and original commits unchanged.

## Recovery and budgets

[Recovery workflow](../../../../../shared/workflows/recovery.md) step 3 explicitly permits a small local defect to return a bounded repair instruction. Its wording is “2 defect cycles per stage **before recovery**”; the frozen RecoveryService likewise returns a bounded repair, rewrite, or pause. Therefore policy `max_review_cycles: 2` has now triggered this assessment; it does not prohibit every repair after independent recovery. Conversely, recording this assessment does not reset any counter or authorize unlimited further repair.

The coordinator should record: two failed R1 cycles, zero completed R2 cycles, this independent recovery invocation, one additional repair allowance, and all prior lineage usage. The next review is cumulative R1 cycle 3, explicitly after recovery; a failed new candidate returns to recovery immediately, with no second repair under this allowance. Count every implementer/reviewer/recovery invocation according to the same lineage accounting convention. Reserve capacity for the implementer, validation if separately delegated, and two fresh independent reviewers before dispatch. No model downgrade or policy waiver is implied.

The selected records have `active_runs: []`, no runtime budget ledger, and a local dispatch record without counters. Before finalization the coordinator supplied a reconstruction from this task's native call metadata: **21 conservatively counted dispatch calls, comprising 13 spawn attempts and 8 follow-ups**, including the rejected recovery spawn and withdrawn-review/advisory follow-ups. Recovery inspected `.ai/local/native-invocations.json` and confirmed those counts; its SHA-256 is `ba24c425e3bf6bbbbdbdf6f39be0c1ca91a8ecb29851b3b27569c54cc239edb6`. This counts the current manual engine run and is not a claim that pre-run design/bootstrap invocations were zero. The coordinator will preserve this accounting and add every subsequent invocation, reserving four further calls for this repair and its validation/reviews within the current run's limit of 300.

For rewrites, the coordinator explicitly chose the conservative count **3 used / 3 allowed / 0 remaining**, retaining all r1-to-r4 transitions regardless of earlier run-bound ambiguity. A local defect repair adds zero structural rewrites. A further structural rewrite therefore requires a durable budget pause and explicit policy authority; no new ID, attempt, or run name resets that allowance. This assessment neither grants a waiver nor promotes incomplete historical invocation counts into a verified global total.

Keep TASK-004 and its descendants undispatched/quiescent while recovery is applied. For a clean recovery boundary, retain a1 through its observed branch/commits and create a fresh TASK-004 attempt/worktree after the coordinator records the decision and verified base. Reuse failed code only as unaccepted salvage with verified ownership; do not merge it as a dependency. Preserve the two failed cycles and lineage counters in the new attempt. A new candidate needs actual current validation, a new fingerprint, fresh R1 and fresh separate R2 on that identical candidate. Source changes invalidate the old command/review binding even though the structural graph stays r4. No graph re-isolation is needed if scope, interface, dependencies, acceptance, and relevant contract semantics remain unchanged.

## Structural fallback, only if the bounded condition fails

The smallest justified alternative is one prerequisite to define an explicit text/reference discriminator for otherwise ambiguous contract entries, plus one replacement for TASK-004 retaining **all AC1/AC2/AC3 obligations** and its bounded installer ownership. Use unused task IDs allocated by the coordinator; do not reuse TASK-004 as new work. Preserve TASK-004 as superseded with its successor mapping, both attempts/reviews, and salvage decisions tied to observed commits. Leave accepted TASK-001 immutable.

That proposal must state the exact legacy-string compatibility/migration rules and digest wire compatibility before implementation, assign exclusive shared-contract/schema ownership only where actually needed, replace every live dependency/handoff reference to TASK-004 with the successor, and order every overlapping reader after the new contract owner. Keep TASK-003 unaccepted and revalidate its relevant context if affected. Preserve all nine plan acceptance mappings. Retain r4 as an immutable snapshot, recompute the entire proposed graph/task identity, validate links/DAG/scope/acceptance/budgets, and send the **whole graph** to a fresh isolation reviewer before the coordinator activates it. No proposed digest, future commit, approval, or unused ID is asserted here. Such a rewrite cannot proceed on an unverified or exhausted rewrite budget.

No structured recovery JSON is emitted: the frozen recovery schema requires a run identity, and no canonical runtime run record exists for this manual execution. The coordinator can preserve this advisory and the explicit conservative counters without fabricating a runtime record or future approval.

## Model provenance

The repository role resolves to OpenAI `recovery`, `gpt-6-astra`, `xhigh`, rank 4. The coordinator confirmed the actual native submission used `gpt-6-astra`, `xhigh`, and a fresh no-fork invocation; the successful dispatch call is `call_iOviw1NG9lbZWqUzd7euZt7u`. This records submitted native tool settings, not independent provider confirmation. This session does not expose a provider-returned effective model ID, reasoning effort, or provider invocation UUID. Source `configured:false` continues to describe unavailable automatic provider bindings. No provider verification, fallback, rank inflation, or schema extension is claimed.

Proceed with the single explicitly recorded bounded repair after the coordinator persists the decision and counters. Escalate only if its exact-recognition boundary cannot preserve the frozen lifecycle acceptance; do not use another broad regex or silently start a third ordinary repair cycle.
