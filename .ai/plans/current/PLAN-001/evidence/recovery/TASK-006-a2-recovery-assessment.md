# TASK-006 a2 independent recovery assessment

Date: 2026-09-08. Role/session: recovery-replanner, `/root/recovery_006_a2`.

**Recommendation: the coordinator may authorize one terminal, precisely limited test-oracle correction in a fresh TASK-006-a3 attempt.** This is a new recovery decision, not continuation of the consumed a2 allowance. Keep graph r4, production bytes, acceptance, permissions and all lineage budgets unchanged. Permit only the inherited-cancellation assertion correction specified below and its handoff. If that candidate encounters any further validation or review failure, pause TASK-006 and its descendants with the evidence; this recommendation grants no subsequent correction, automatic new recovery allowance, or structural rewrite. If the coordinator declines this additional exception, the present disposition is a pause, not permission to retry a2.

This assessment does not accept source, authorize a repair itself, or establish a passing candidate. No final a2 candidate manifest/fingerprint or c3 R1/R2 exists.

## Evidence and classification

I read the selected plan/spec/task, approved r4 graph and isolation report, ADR-001–005, frozen service boundaries and relevant command schema, accepted TASK-002/004/038 handoffs, policy/model mapping, recovery role and recovery/review workflows. The named a1 c1/c2 reports and first recovery were loaded to determine cumulative failure and allowance history. The decisive current evidence is the [a2 validation failure](TASK-006-a2-validation-failure.md), its raw [Windows log](../validation/TASK-006-a2-4fe9e7a30378.txt) and [Linux log](../validation/TASK-006-a2-4fe9e7a30378-linux.txt), the retained a2 source, and the actual a1-to-a2 test diff.

| Retained event | Established fact |
| --- | --- |
| a1 c1 R1 | FAIL: unbounded inherited-pipe return and an incorrect POSIX cleanup claim. No c1 R2. |
| a1 c2 R1 | PASS: both production findings resolved on that exact candidate. |
| a1 c2 R2 | FAIL at R2-10: two blind cancellation timers precede readiness on Linux 3.11. |
| First recovery and coordinator decision | Exactly one a2 test/handoff repair; production frozen; any candidate validation/review failure returns to recovery, with no second repair supplied by that allowance. |
| a2 owner handoff | Four platform/version passes reported and retained as prior owner evidence. |
| a2 coordinator Windows 3.12.14 | Exit 0, 22 discovered, 21 non-skipped, one established platform skip. |
| a2 coordinator Linux 3.12.3 | Exit 1, 22 discovered, 21 non-skipped, one established skip, one inherited-cancellation assertion failure. |

Both coordinator origin probes passed, used jsonschema 4.26.0, and located commands.py in the exact a2 tree. The coordinator stopped without rerunning the failure or running either remaining 3.11 stage. The owner results do not supersede this failed exact validation.

**Classification: flawed completion of the bounded test repair, specifically a residual incidental-duration oracle.** This is an owned test defect. The fast Linux observation is new evidence of that defect, not a newly missing environment, a new product requirement, or a demonstrated decomposition defect. The two startup timers were replaced with observed fixture phases, but the inherited test retained exactly the old expression at line 799:

```python
self.assertGreaterEqual(elapsed, 0.75 if mode == "timeout" else 0.15)
```

The [read-only verification script](TASK-006-a2-recovery-assessment-verify.py) compares the old and new syntax trees and confirms that assertion is unchanged. Once cancellation depends on readiness rather than a 250 ms timer, no command contract requires startup plus cancellation response to last at least 150 ms. The retained floor can reject correct, faster execution. Merely rerunning the same test until scheduling makes it slower would not correct this defect.

The failing Linux inherited-cancellation subcase records phase `inherited_after_parent_exit`, readiness at 0.051996 s and cancellation response of 0.057143 s. Its unrounded elapsed time is 0.10913950300891884 s, below 0.15 s. The fixture required a live exact descendant with open inherited descriptors, an exited launched parent, and the retained parent group before requesting cancellation. It recorded one native termination call. Its `finally` summary confirms both observed PID identities gone, command readers settled and no watchdog intervention.

Those cleanup facts are teardown observations. They do not establish whether the descendant was already gone before teardown, or prove the returned status, EOF/truncation flags, durable log contents, or schema validity. The assertion at line 799 precedes those later assertions, which did not execute in this failing subcase. Even the later explicit response-bound assertion was not reached. The recorded response is diagnostic evidence, not a substitute for a completed test. No production defect is established by this failure alone, and production acceptance remains unresolved until fresh reviews.

The a2 source does contain the previously required readiness observer, native delegating terminator, distinct startup/response/overall measurements, exact parent/PID registration, unconditional cleanup and failure-only watchdog. Their presence and the observed successful phase transition distinguish this failure from a repeat of the missing-PID race. The retained helper is substantial; this assessment does not approve every failure path. Any newly demonstrated helper, cleanup, phase, or production defect exceeds the surgical instruction below and requires the stated pause.

## Why a final bounded instruction is defensible

The [recovery workflow](../../../../../shared/workflows/recovery.md) explicitly says a small local defect returns a bounded repair instruction, while requiring retained lineage and a durable pause on exhausted budgets. The frozen `RecoveryService.recover` contract permits bounded repair or pause. Neither record imposes a separate numeric maximum on independent recovery diagnoses; neither grants unlimited repair loops. The [first coordinator decision](TASK-006-coordinator-decision.md) required this failure to return to recovery and expressly supplied no second repair. That allowance is fully consumed and remains failed.

A new coordinator decision can therefore consider a narrower instruction after this independent diagnosis. The justification is the exact demonstrated contradiction between the retained floor and the now-observed cancellation event, with a correction limited to one existing assertion region. It is not the user's desire for completion alone, spare invocation budget alone, or the owner's previous passes. Graph r4 still supplies the correct component, prerequisites and ownership; splitting tasks would not address this expression and would consume an unavailable fourth rewrite.

Alternatives rejected: accepting the current failure; treating the old R1 as approval for changed tests; delaying cancellation or increasing a timer to satisfy 0.15 s; lowering the floor to another machine-dependent constant; skipping the subcase; broad fixture redesign; production changes; a scope/dependency/contract rewrite. A pause is a valid fallback if the narrowly specified instruction cannot be adopted or satisfied. An unbounded series of further local exceptions is not recommended.

## Exact proposed correction and preserved obligations

After a fresh owner salvages the frozen a2 bytes, only the assertion region in `test_inherited_pipe_descendant_cannot_block_timeout_or_cancellation_return` and the owned TASK-006 handoff may change. Do not edit the helper classes, watchdog, detached test, other tests, command definition, or any production file under this instruction.

1. Make `elapsed >= 0.75` apply only to the real one-second timeout subcase. Preserve that value and the timeout definition. For cancellation, replace the arbitrary elapsed floor with explicit assertions on the existing observed monotonic facts: `started <= phase_observed <= cancellation <= completed`, all required observations present, and a nonnegative cancellation response. Retain the strict response bound `< 3.0 s`, existing elapsed bound `< 4.0 s`, three-second startup bound and six-second failure-only watchdog. Do not insert a wait or a new positive minimum duration. This preserves meaningful active cancellation through its observed cause rather than elapsed startup time.
2. Preserve both fixture phases and all four subcases. Inherited cancellation must still observe the live pipe-holding descendant after the exact parent exits; detached cancellation must still observe the live detached descendant with the parent live, including Linux group/session separation. Preserve the original real timeout behavior and native termination delegation; no fake cleanup result, missing-PID suppression, readiness skip or pre-return external killer can support a pass.
3. Preserve every current native status, error, exit-code, log, EOF/truncation and schema assertion. Unproved cleanup remains `unknown/ambiguous_side_effect` with null exit; Windows confirmed live-parent timeout/cancellation retains its native status expectations. Inherited EOF on Linux and incomplete capture on Windows remain distinct. The corrected inherited-cancellation path must reach and execute these formerly blocked assertions.
4. Preserve immediate exact parent/descendant registration and unconditional bounded `finally` teardown, including readiness/assertion failure paths. Confirm all observed identities gone, reader and test execution/signaling threads settled before fixture removal. Finite fixture lifetime is only a fallback. Any failure-only watchdog intervention is an unconditional failure; unknown cleanup or an unsettled thread cannot be relabeled as success. If verification exposes a need to redesign that machinery, stop rather than expanding this allowance.
5. The handoff must name this second recovery decision and the failed a2 validation, explain why event ordering replaces the cancellation floor, retain all older observations without relabeling them, and report actual new results and source preservation. Do not claim the old failing subcase already passed its later assertions.

Task acceptance is unchanged: TASK-006-AC1 retains fixed argv, cwd policy, actual timeout/exit/time facts; TASK-006-AC2 retains output handling, failure and meaningful active cancellation. Plan AC-02 and the AC-09/ADR-001 supported Python/platform expectation remain; AC-05 still requires both independent reviews. `execute(CommandRequest) -> CommandEvidence`, accepted saved-settings semantics, explicit permissions/environment, offline schemas, TASK-018 success-rule ownership and TASK-034 wiring ownership remain frozen. All other plan criteria remain required.

## Fresh ownership, validation, lineage and stop

Only after this assessment is final and the coordinator records a new explicit decision may it dispatch a distinct fresh Sol/xhigh implementer for TASK-006-a3, in a new branch/worktree beneath `.worktrees/`. Verify the latest ROOT base, accepted dependency ancestry, a quiescent a1/a2 ownership boundary and exclusive original three-path / `component:commands` ownership. The current independent TASK-024 owner may continue within its separate ownership. No runtime lease or run identity is invented by this assessment.

The following exact a2 commits are retained source salvage, oldest first:

- `60953e6aa33ad45680e1bb7e9873289fbb1d1186`
- `1403be69fdab0e753a608bd815b29d8f540a7067`
- `e58bb0c699df063c9577ccd8811dce4adb6984c1`
- `5e455de46416758f0438c95ecda71c0630dc64a8`

They may be cherry-picked only as unaccepted salvage under the new decision. Do not merge the failed attempt as accepted dependency context. Before the one correction, verify the three task-owned paths match frozen a2. Before and after correction, commands.py must remain Git blob `39828108eb7d198d81d810c8fd9e136e3f64f8f0` / raw SHA-256 `700079bb47853a1d4cdebaa89bafcacf03885a81f9ef57687ec7203338ec29d0`. All other production bytes must match the verified fresh base; the post-salvage test diff must be confined to the assertion region above. Retain clean a1/a2 branches, commits and all first-recovery/candidate/review/failure files.

After the single correction, freeze the owner source and run the exact declared argv `python -m unittest discover -s tests/unit/commands/ -p test_*.py` on Windows 3.11, Windows 3.12, Linux 3.11 and Linux 3.12. Preserve actual interpreter/version/platform, dependency and candidate module origins, exits, 22 discovered / 21 non-skipped / one established skip, and all four phase/response/cleanup summaries. Both formerly repaired cancellation cases must execute. No new skip or lowered count is permitted. Run the foundation validator and exact diff checks. The coordinator must independently validate the exact current-base snapshot through all four environments, binding those actual results before computing its candidate fingerprint. WSL evidence remains local Linux evidence, not final platform CI.

Only a passing exact candidate can enter fresh cumulative **c3 R1**, followed after its pass by a separate fresh **c3 R2**, both Astra/xhigh and distinct from all implementers and prior reviewers. Both must bind the identical candidate, all current context/validation hashes and the preserved failure history. The old c2 R1 cannot transfer. R1 must assess phase, native behavior, EOF/capture, watchdog and failure-path cleanup; R2 must independently verify that the changed oracle asserts the intended behavior and preserves the frozen contracts. Material source, test, base or relevant context changes invalidate candidate-bound results and reviews.

**Terminal stop:** any further candidate test/validation failure, R1/R2 failure, unproved cleanup/phase, unexpected diff or need for broader source work ends this proposed correction path. Preserve the exact failure and pause affected work with a concrete next action; do not rerun to obtain a passing schedule, mutate the frozen candidate, dispatch a further repair, waive a gate, reset a cycle, or produce a fourth graph rewrite under this recommendation. The coordinator can continue independently ready work, but this assessment supplies no automatic next repair. It must not mark PLAN-001 complete while TASK-006 or descendants remain unaccepted.

## Verified identities, budgets and assessment checks

The [verification output](TASK-006-a2-recovery-assessment-verify.txt) exited 0. It verifies ROOT `60f8801ac5679b4233aebdcb58f291e51a672e39`, clean retained a2 head `4fe9e7a30378e13b43dc51745593adae2bb8b051`, reviewed-source base `17595809d6ee74b2585265d94535cb1630d3a900`, and owner FINAL source commit `5e455de46416758f0438c95ecda71c0630dc64a8`. The metadata merge did not change any of the three owned files. Clean a1 remains `ce4c8edb86b02268a856a5f870932ade0e7e9b91`. The base-to-a2 binary diff has only the original three paths and SHA-256 `7e50dd79d2a38622fa380ab2f7a31c7f258a701006cb9cd8b11716c7a765488d`.

Verification also confirms accepted 002/004/038 ancestry and unchanged source/handoffs; frozen policy/models, service contracts, schemas, plan/spec and command; the 39-task r4 digest `c84fdf4e0affcb8d329e8fc7ce2ae928410b44a21238304b3348b2e7d1b75c9e`; 57 earlier TASK-006 evidence/history files unchanged; the three preserved formal review verdicts; all 14 old committed context hashes and three bound validation hashes; and both current raw log hashes. Generation remains 33, with 9/39 accepted, no runtime active run and all 23 transitive TASK-006 descendants unaccepted with no attempt IDs.

The coordinator-provided [native snapshot](TASK-006-a2-native-invocations-74.txt) independently verifies SHA-256 `138b44d899472f7e8d9b88db5f652ce34e4e815769af6befb6e0c674ae1c74c1`, 73 distinct observed spawn/follow-up attempts including rejected calls plus the continuing coordinator: **74/300 charged, 226 remaining**. A fresh owner and two fresh reviews would make **77/300 charged or reserved, 223 remaining**, before any other invocation. Historical rewrites remain **3/3 used, zero remaining**; this proposed local instruction consumes zero rewrites. `max_review_cycles: 2` is unchanged. Preserve c1 R1 FAIL, c2 R1 PASS/R2 FAIL, and one failed post-recovery a2 validation allowance; the proposed a3 is not a fresh lineage or a counter reset.

This recovery submission is the snapshot's `call_k8j0CnC7m99QUnjtTbjyaNnW`, observed OpenAI `gpt-6-astra` / `xhigh`, recovery rank 4. A coordinator message supplied another call string and then corrected it; only the verified snapshot row is used here. These are observed submission settings, not provider-effective model/effort confirmation or a provider invocation UUID. Automatic profile bindings remain false, consistent with the [provenance clarification](../effort-provenance-clarification.md). No schema-backed recovery JSON is fabricated without an actual runtime run identity.

Assessment validation is read-only identity/source/log analysis, exact diff checks, and the [foundation run](TASK-006-a2-recovery-assessment-foundation.txt). Foundation exited 0 with 27 schemas, 174 artifacts, 39 tasks, 280 unordered pairs, four archive manifests and 387 links. No fixture suite was rerun, no hypothetical corrected test was executed, and no later status/EOF/schema check is inferred from a failed path. The foundation result checks repository records and links; it does not accept this implementation. This role writes only this assessment and same-stem companions. Source, canonical records, refs, previous evidence and the coordinator's native snapshot remain untouched. All worktree access stops before FINAL; the coordinator owns the next decision.
