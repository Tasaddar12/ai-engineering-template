# TASK-024 coordination reminders

Dispatch only after accepted013/014/039. Own recovery_proposals.py, the declared
test leaf and TASK-024 handoff. This is pure proposal validation;025 owns active
recovery orchestration, quiescence, isolation invocation and durable application.
Do not invoke agents, alter the actual current graph or implement025 as a shortcut.

Consume actual accepted graph/scope services and039 typed graph/task/recovery,
acceptance-mapping and lineage-budget values. A scoped immutable proposal input
and validation result may refine these values without changing v1 fields or the
frozen RecoveryService signature. Validate the proposed whole live topology using
the accepted013 semantics and preserve old/superseded/archived history separately;
old IDs are never reusable merely because they are absent from the live DAG.
The current RecoveryRequest's live snapshots are intentionally not the entire
historical registry. Accept explicit verified historical/full-record material via
a scoped internal input or injected collaborator; existing content refs bind it.
Do not add history/status/description fields to frozen039 or v1 records.

Exercise split, replace, sequence and augment with complete successor mapping,
acceptance preservation and dependent-edge redirection. Acceptance identifiers
alone must not hide changing the original success descriptions. Completed task
facts remain immutable; newly required work gets fresh identities. Reject dangling
or superseded-node dependencies, cycles, omitted criteria, plan mismatches and ID
reuse across known lineage. Actual039 RecoveryAction also has repair and merge;
support or explicitly reject actions outside this task's documented scope rather
than silently treating them as one of the required four.

Use014 conflicts with actual supplied reachability facts from013 to detect
unsequenced path/read/resource ownership collisions. A proposal validator's pass
does not equal an independent isolation-review pass or a committed graph change.
Permission expansion and lineage-budget reset/increased limits require authority
outside recovery. Preserve actual observed usage even if already above a limit:
039 intentionally represents overshoot; it must lead to a typed rejection/pause
at the appropriate service rather than construction failure or clamped counters.
No new task ID resets cumulative invocations, tokens, elapsed time or rewrites.

Validate all applicable task/spec constraints with meaningful positive and negative
fixtures, including preserved historical bytes/IDs, same local IDs in different
plan namespaces, changed completed content and actual accepted graph/scope APIs.
Keep unsupported or missing facts explicit. Document pure producer/admission
boundaries so025 can safely consume the result without inventing stronger proof.
