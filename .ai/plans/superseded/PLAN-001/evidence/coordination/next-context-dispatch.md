# TASK-016 coordination reminders

Use only accepted003/004/038 and the frozen ContextBuilder.build(ContextRequest)
-> ContextBundle contract. Own context.py, context test leaf and TASK-016 handoff.
Resolve the explicit document, dependency/sibling handoff, interface, R1 and optional
references supplied for the selected role. Missing required references block;
never compensate by loading the whole repository, all plans or archived history.

Read only authorized project-relative files with real containment checks. Reject
absolute paths, traversal, aliases and links/junctions that escape their roots.
Keep local host bindings out of the portable manifest. Required secret/path leakage
must fail explicitly with sanitized errors; do not hide a dropped acceptance input
behind required_context_complete=True. The selected task/plan identity and
acceptance IDs remain intact, and unrelated plan handoffs cannot satisfy required
inputs merely by sharing a local task ID.

Hash actual selected bytes and construct deterministic, schema-valid manifests
without self-reference. Define token estimation and ordering/tie-break rules
explicitly. The output calls its count estimated_tokens; do not claim a real model
tokenizer was invoked if using a local conservative estimator. Retain all required
context and acceptance; omit only explicitly optional references and record every
omission. If required material alone exceeds budget, return a typed budget error.

Test real disposable files, deterministic order/repetition, material-byte changes,
required/optional budget boundaries, missing required refs, duplicate or cross-plan
identities, secret/error sanitization, traversal and actual supported link guards.
No provider dispatch, workflow state mutation, schema edits, shared fixture or
unowned modules. Record concrete limits and observable counts rather than claiming
unexecuted platform behavior.
