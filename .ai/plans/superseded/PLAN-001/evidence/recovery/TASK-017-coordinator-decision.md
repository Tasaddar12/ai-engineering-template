# TASK-017 coordinator recovery decision

Adopt the independent [recovery assessment](TASK-017-recovery-assessment.md.txt)
in full and authorize one bounded repair in fresh attempt TASK-017-a2. The
coordinator read its complete assessment, independent verification and final
checks after the recovery role stopped all access. The observed mapped-profile
restart failure is local to agents.py; accepted configuration already represents
the required relationship. No graph, scope, dependency, acceptance, permission,
port or schema change is justified.

The first coordinator foundation check rejected the assessment's diagnostic
JSON as a live artifact because it has no v1 artifact kind. The coordinator
preserved its bytes as `.json.txt` and preserved the exact submitted assessment
as `.md.txt`, whose original relative links are historical. No recommendation,
observation, verdict or report content was edited. The [packaging record](TASK-017-evidence-packaging.md)
maps the original names to their retained byte-identical files and verification
hashes. This is evidence packaging, not another task repair or review waiver.

The verified ROOT is `10b408fa0fd43eafa49acc88dec2c5976be05966`, generation38,
with ten of39 tasks accepted. The clean failed a1 remains
`e79df3b8d071a7e3a3c7874cf0cacb6e704c0205` on its retained branch/worktree.
Both failed R1 cycles and all20 historical review companions remain unchanged.
There has been no TASK-017 R2. The fourteen dependent tasks remain backlog with
no attempts; independent TASK-007 and TASK-018 may continue.

After this decision checkpoint, create a2 from current accepted ROOT and let a
fresh Sol/xhigh owner cherry-pick only these verified owned commits, oldest first:

- `71e0ccf697c132082bb17f7f3814ffb6abee5df6`
- `7e5bd5fd5d1b36a14cbee6ae911b68a4ec9ffbf8`

Verify that the three salvaged source/test/handoff blobs match frozen a1 before
repair. Record original-to-salvage commit mapping. Do not merge a1 wholesale or
present its unaccepted code as a delivered dependency. Only src/agents.py,
tests/unit/agents/, and the TASK-017 implementation handoff may change.

The assessment's five-part repair contract is binding. Keep requested policy
profile, resolved provider profile and expected simulated identity distinct.
Retain intrinsic private-state shape/history/identity checks; validate the entire
selected provider profile against the saved effect at the settings-bearing
admission/use boundary through the accepted configured_model mapping. Preserve
provider/profile names, model, rank and both effort fields in that comparison.
Do not weaken admission so a saved handle bypasses the binding check. Forged or
stale provider-profile names must still reject there, rather than reinstating
the invalid constructor spelling equality. Keep terminal results, cancellation,
cursor fences, all consumed-history checks and legitimate queued/late/exhausted/
partial/fault-injection recovery behavior.

Add real-loader fresh-process mapped-profile and identity-control regressions,
with identical settings bytes, one recovered effect/handle, separate requested
and resolved names and normal continuation. Include negative selected-binding,
effort and provenance checks with unchanged saved bytes/cursors on rejection.
Use accepted TASK-003/004/038 unchanged. No canonical journal, leases, production
provider, multi-owner lock, native-setting edits or new wire fields are authorized.

Run the exact declared nonzero suite from a2 on Windows3.12, Windows3.11 and
Linux3.11 with actual candidate module origins, plus appropriate foundation/diff
checks. Retain actual intermediate failures and final results honestly. The
coordinator forms a current-base candidate only after the owner is final and
clean, binds fresh validation before fingerprinting, and requires cumulative
c3 fresh Astra/xhigh R1 followed on PASS by a distinct fresh R2 on that exact
identity. Old review results do not transfer. A material base/source/context
change invalidates both gates. Acceptance is still a later observed decision.

This grants one a2 repair candidate, not another ordinary repair loop. If the
bounded correction cannot satisfy the contract or its candidate validation or
formal R1/R2 fails, preserve evidence and return to independent recovery before
any further repair. Do not run R2 after a failed R1. Structural requirements
cannot consume a nonexistent fourth rewrite; record a concrete budget pause
instead. No counter, rank, permission, task ID or run identity is reset.

The preserved [83](TASK-017-native-invocations-83.txt) and
[87](TASK-017-native-invocations-87.txt) snapshots were verified. Current charges
are87/300, including the interrupted recovery and resumed workers; reserve one
owner and two reviews for90 charged or reserved. Historical rewrites remain3/3
used, zero remaining. Native submission/inherited model and effort are observed;
separate provider-effective identity remains unavailable. No reset credit was
purchased or consumed by the coordinator. The usage interruptions remain history.

Generation38 advances to39 and TASK-017 becomes ready only for this bounded a2
dispatch. Local policy already authorizes this correction; no routine user
approval is required. The unrelated [obsolete checkout cleanup](../worktree-cleanup-2026-09-08.md)
removed only clean old TASK-004/006 directories after their later attempts were
accepted, retaining exact branches/commits and all120 review/recovery files.
Current a1 and all active/queued source remain intact. The entire39-task user
request, integrated validation and final independent review remain in progress.
