# TASK-004 coordinator recovery decision

Adopt the independent [recovery assessment](TASK-004-recovery-assessment.md) for
one bounded local repair. Graph r4, task scope, dependencies, all acceptance
criteria and public contract semantics remain unchanged. Closed whole-value
recognition of documented known record paths replaces guessing in both mixed
contract arrays; unmatched text remains byte-exact.

Both failed R1 reports and exact candidates are preserved. TASK-004 and all its
descendants remain unaccepted and undispatched. The clean a1 branch retains frozen
head `248f4dedf5d37d09eb27a3ef08bb63a570359666`. A new a2 worktree starts from the
coordinator decision checkpoint. Source commits `510c193` and `6206a07` may be
cherry-picked into a2 as unaccepted salvage; ownership is checked against the
original five task paths. No failing source is integrated as a dependency.

The [budget checkpoint](manual-budget-2026-09-08.md) charges prior usage and
reserves the repair/reviews. The allowance is exactly one repair followed by fresh
R1 and fresh independent R2 on one current candidate. Any further failure returns
to recovery immediately; no counters reset, acceptance waiver, or ordinary retry
is authorized. An unresolved arbitrary path/prose representation requirement is
a structural discovery and cannot consume an unavailable fourth rewrite.

Coordinator validates the declared suite at the new exact commit before candidate
formation. Independent reviews must verify all retained criteria, regression
reproductions, genuine relocation, prose invalidation and standalone installation.
TASK-003 may proceed independently after this checkpoint.
