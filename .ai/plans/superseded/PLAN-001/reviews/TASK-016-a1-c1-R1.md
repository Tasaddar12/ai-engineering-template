# TASK-016 a1 c1 — independent implementation review

**FAIL.** Required handoff and R1 records can pass with missing frozen schema fields, and DTO-accepted non-UTF-8 metadata escapes as an untyped encoding exception. All eleven applicable implementation checks and relevant accepted-interface/plan consistency checks are complete. No area remains unreviewed because of these findings. This is the [single task review stage](../evidence/coordination/single-stage-review-decision.md); no separate task R2 occurred.

| Binding | Verified value |
| --- | --- |
| Frozen ROOT / base | `e44eeb2b4197774a57d03918518381962aa37c20` |
| Clean candidate | `e88e7950fb6bce3656d3de8dad54ace21074d252` |
| Tested owner head | `0cb81d179b7ffd76012f05f216010ae444f11f98` |
| Fingerprint | `160170998f8906e5c944f91cb0e8c60c92fda221d515e2e5d21f9aabc848a5cc` |
| Manifest | [CANDIDATE-TASK-016-a1-e88e7950fb6b.json](candidates/CANDIDATE-TASK-016-a1-e88e7950fb6b.json) |
| Reviewer | Fresh `/root/review_016_c1`; native dispatch `call_HdCiOZGmqwHKAE7E7lvfgzu4`, charge 120/300 |
| Latest implementation owner | `/root/repair_019_c1`; native follow-up `call_NKsLx2ySQMvDK71Z8WIk7daz`, charge 112/300 |

The coordinator observed explicit OpenAI `gpt-6-astra` / `xhigh`, review_high rank 4, separate from the inherited Sol/xhigh rank-3 implementation owner. These are native submission observations. No provider-returned effective model identity, effort attestation or production provider run is claimed. Automatic policy profiles remain `configured: false`; the [effort clarification](../evidence/effort-provenance-clarification.md) and current manual decision apply without changing those records.

The [identity audit](TASK-016-a1-c1-R1-identity.json.txt) verifies the raw binary diff digest, all 15 committed context hashes against candidate bytes and frozen ROOT committed bytes, current ROOT validation hash, raw policy/model concatenation digest, unsigned canonical fingerprint, schema-valid candidate, approved r4 and 39-task structural digest, and accepted TASK-003/004/038 ancestry, status and source/handoff blobs. ROOT working CRLF bytes were not substituted for committed context bytes. Exactly the three owned files differ from base: context source, its test leaf and its handoff. Those three match the tested owner head exactly. The newer merge also adds other accepted components and tests, all verified byte-identical to frozen ROOT; the entire owner-to-candidate merge is not described as metadata-only.

| Category | Location | Exact issue | Required fix |
| --- | --- | --- | --- |
| Contract | `src/context.py:560`, `:586`, `:623`; `tests/unit/context/test_context.py:67`, `:82`; handoff Public API step 3 | **R1-TASK-016-001 (major):** A required handoff containing only `schema_version`, `kind`, `id`, `task_id`, `commit_oid`, or an R1 record containing only `schema_version`, `kind`, `plan_id`, `task_id`, `stage`, fails the accepted v1 registry but produces `required_context_complete=True`. Missing handoff attempt/base/context fields and missing review candidate/fingerprint/verdict/check evidence are never validated. The owned positive fixtures use these incomplete records, so they conceal the mismatch. | Validate complete structured handoff/R1 records against the accepted v1 contracts before selecting them as required context, preserving sanitized failures and the existing build port. Use schema-valid positive fixtures and tracked negative regressions for missing/invalid/unknown fields in both record kinds. Align the handoff with actual validation. Keep acceptedness with the curator/coordinator; add no wire field. |
| Bug | `src/context.py:354`, `:101`, `:707`; `tests/unit/context/test_context.py:296` | **R1-TASK-016-002 (major):** The actual accepted `ContextRequest` allows `role='reviewer\ud800'`. Screening accepts it, the file is read, then canonical UTF-8 encoding raises `UnicodeEncodeError` instead of the promised sanitized `DomainException`. Required context construction can therefore terminate outside its typed error boundary. | Reject metadata that cannot be represented in the supported UTF-8 encoding with a sanitized typed error before reads, covering the request metadata used by canonical serialization. Preserve valid metadata bytes. Add tracked regression coverage through the actual DTO for role and acceptance metadata, and any other affected serialized fields. |

The record finding concerns schema admission, not dependency acceptedness or matching a review to a candidate identity absent from `ContextRequest`. The frozen handoff has no plan_id or accepted-status field. The existing selected-plan physical qualification, distinct/non-self task checks and matching review plan/task/stage remain useful and must stay intact. Full schema-valid handoff and review controls pass independently through both the registry and builder.

| Check | Result | Evidence and reasoning |
| --- | --- | --- |
| R1-01 acceptance | Fail | AC1 raw hashes and canonical ordering pass, but malformed required typed records satisfy completeness (001). AC2 valid UTF-8 budgets, required acceptance retention and known-value rejection pass; accepted non-UTF-8 metadata lacks a typed rejection (002). |
| R1-02 spec/exclusions | Pass | Bounded explicit context implements the REQ-04/AC-04 slice. No provider effects, state writes, unaccepted imports, shared contract edits or orchestration behavior are introduced. Runtime two-review contracts remain unchanged under the manual single-stage decision. |
| R1-03 correctness | Fail | Actual raw byte hashes and an independently assembled unsigned manifest preimage match. Required-first selection and deterministic optional omissions pass. Structured-record admission and encoding failures remain (001–002). |
| R1-04 errors/cleanup | Fail | File handles use a context manager; missing, binary, oversized, denied and leaky sources map to sanitized typed errors. DTO-accepted surrogate metadata escapes that boundary (002). Disposable test fixtures clean up. |
| R1-05 boundaries | Fail | Exact required multi-file budget and one-less rejection, metadata-dominated budgets, later small optional after earlier omission, scope prohibition, NFC/case aliases, cross-plan references and record identity negatives pass. Malformed schema fields and UTF-8 metadata remain gaps (001–002). |
| R1-06 tests | Fail | Declared 18-test suite passes with one real Windows privilege skip, and Linux independently executes the real link guard. Tests omit malformed required-schema rejection and surrogate metadata, and use incomplete record positives (001–002). Necessary regressions belong in the owned tracked leaf, not this diagnostic. |
| R1-07 scope | Pass | One flat implementation module, one owned test file and one handoff. Runtime imports only standard library and accepted domain/workflow values. No ignored/local/runtime helper dependency or extra public contract. |
| R1-08 unrelated changes | Pass | Exact owned base diff, clean candidate, owner byte equality, dependency blobs, graph/context/policy/evidence identity and accepted integration additions verified. |
| R1-09 interfaces | Fail | The real runtime-checkable ContextBuilder signature and immutable ContextRequest/ContextBundle are used; output passes ContractRegistry. Structured inputs that the same accepted registry rejects are admitted as complete (001). No schema migration is required. |
| R1-10 trust boundaries | Fail | Exact supplied sensitive values and host paths in role, acceptance, content and reference paths are rejected/omitted with safe diagnostics; metadata screening precedes reads. Explicit reads, containment and real escaping-file/in-root-directory symlinks are guarded. Malformed required review/handoff records still cross the typed input boundary (001). No claim of complete detection for arbitrary unknown prose or hostile concurrent filesystem mutation. |
| R1-11 documentation | Fail | Full existing handoff was read, including owner fixes, command failures/skips and limits. Its v1-record and sanitized-error claims overstate implemented behavior (001–002); update alongside the fixes. Scope, acceptedness responsibility, local provenance and provider limits are otherwise accurate. |

The single [independent diagnostic](TASK-016-a1-c1-R1-probe.py) uses actual accepted ports and registry with disposable files; it imports no owned test fixtures. Its [captured result](TASK-016-a1-c1-R1-probe.json.txt) records **six passing groups and three failing groups**, exit 1. The two invalid-record failures form finding 001; the UnicodeEncodeError forms 002. Checks continued after each failure. A 2,000-level JSON control returned a typed invalid-handoff error on this runtime; no broader parser-depth guarantee is inferred.

The valid full-record control measured 1,770 selected source bytes, reported 2,844 estimated tokens, and produced a 944-byte complete wire bundle. Raw hashes and the final digest reconstructed independently. Required inputs succeed at the reported 2,844 boundary and reject at 2,843; acceptance metadata alone can exhaust budget; optional omission paths are counted. The estimator counts its documented private payload and selected bytes, not a model tokenizer. Its count excludes final self-derived id/digest and final numeric fields while including extra request/scope metadata; the tested complete wire bundle plus selected bytes remains covered. No source truncation or silent required omission was observed.

| Independent command | Actual result |
| --- | --- |
| Windows coordinator Python 3.12.14, exact `-m unittest discover -s tests/unit/context/ -p test_*.py` from candidate | [Captured declared result](TASK-016-a1-c1-R1-declared.json.txt): exit 0; **18 tests in 0.377s**, `OK (skipped=1)`. Candidate source origins are independently recorded in the diagnostic. |
| WSL Ubuntu-24.04, project Linux Python 3.11, same leaf with `-k real_file_and_directory_links -v` | [Captured link result](TASK-016-a1-c1-R1-linux-link.json.txt): exit 0; **1 test in 0.002s**, zero skips. Real escaping file symlink and in-root directory symlink were created and rejected. |
| Coordinator Python `src/validate_foundation.py` from candidate | [Captured foundation result](TASK-016-a1-c1-R1-foundation.json.txt): exit 0; 27 schemas, 207 artifacts, 1 plan, 39 tasks, 280 pairs, 4 archives and 598 links. This checks records, not product behavior. |
| Review program `identity` / `behavior` | Identity passed; behavior exited 1 with the findings above. [Setup failures](TASK-016-a1-c1-R1-setup-failures.txt) preserve an initial reviewer import-path mistake and an overbroad owner-diff assertion, both corrected only in review evidence. |
| `git diff --check` on exact base/candidate | Exit 0, no output; three owned additions, no rename/deletion or whitespace errors. |

The bound coordinator run reports 18 tests in 0.354s with one skip. The owner's Windows 3.11/3.12 and full Linux 18-test runs remain owner evidence and are not relabeled independent. Only the Linux link lane was repeated to close the actual Windows capability gap; no unchanged broad platform matrix was rerun. Current foundation and source inspection confirm the implementation has no product dependency on this diagnostic or ignored `.ai/local`.

Return to the owner for the two scoped fixes and tracked regressions. Keep this failure immutable, validate a new exact candidate and obtain focused independent verification within the same stage. The [structured report](TASK-016-a1-c1-R1.json) and [closing verification](TASK-016-a1-c1-R1-verification.json.txt) bind the outcome. Closing rechecks frozen material and report schema/evidence links; only the disclosed ROOT CURRENT.md update is outside the manifest. All ROOT/candidate access stops after that closing check.
