---
question: Which official command semantics should bind safe, resumable delivery?
evidence_and_sources: 'Git remote get-url expands insteadOf/pushInsteadOf, defaults
  to the first URL, selects push URLs with --push, and returns every URL with --all.
  [Git documentation](https://git-scm.com/docs/git-remote).


  GH_REPO supplies repository context; GH_HOST supplies a host where one was not provided
  or inferred. [GitHub CLI environment manual](https://cli.github.com/manual/gh_help_environment).


  PR creation supports explicit --repo [HOST/]OWNER/REPO and --head; --head skips
  automatic forking/pushing and supports user:branch. Organization owners are currently
  unsupported in that head-owner syntax. The CLI dry-run flag can still push, so it
  cannot implement this framework''s effect-free dry run. [GitHub CLI PR manual](https://cli.github.com/manual/gh_pr_create).'
conclusions: Resolve all effective push destinations and require an explicit, stable
  PR repository identity. Define cross-fork support explicitly. The framework must
  implement dry-run without calling external tooling. These are engineering inferences
  from the cited command semantics.
uncertainties: No live remote or credential behavior was exercised. Controlled integration
  tests verify the framework's adapter decisions; future CLI releases may change limitations.
checked_at: '2026-09-09T01:46:54.208609+00:00'
context:
- .ai/plans/active/PLAN-002.md
id: RES-002
kind: research
title: Git and GitHub CLI delivery identity semantics
status: active
---
# RES-002 — Git and GitHub CLI delivery identity semantics

## Question

Which official command semantics should bind safe, resumable delivery?

## Evidence and sources

Git remote get-url expands insteadOf/pushInsteadOf, defaults to the first URL, selects push URLs with --push, and returns every URL with --all. [Git documentation](https://git-scm.com/docs/git-remote).

GH_REPO supplies repository context; GH_HOST supplies a host where one was not provided or inferred. [GitHub CLI environment manual](https://cli.github.com/manual/gh_help_environment).

PR creation supports explicit --repo [HOST/]OWNER/REPO and --head; --head skips automatic forking/pushing and supports user:branch. Organization owners are currently unsupported in that head-owner syntax. The CLI dry-run flag can still push, so it cannot implement this framework's effect-free dry run. [GitHub CLI PR manual](https://cli.github.com/manual/gh_pr_create).

## Conclusions

Resolve all effective push destinations and require an explicit, stable PR repository identity. Define cross-fork support explicitly. The framework must implement dry-run without calling external tooling. These are engineering inferences from the cited command semantics.

## Uncertainties

No live remote or credential behavior was exercised. Controlled integration tests verify the framework's adapter decisions; future CLI releases may change limitations.

