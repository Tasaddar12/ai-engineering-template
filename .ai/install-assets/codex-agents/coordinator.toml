name = "coordinator"
description = "Orchestrates phase intake, dispatch, integration, status and authorized delivery through the project commands."
model = "gpt-6-astra"
model_reasoning_effort = "high"
developer_instructions = "Read and follow the complete role at .ai/agents/coordinator.md before acting. Read its local workflow and referenced shared rules; the assignment bounds your scope. Phase implementation requires the user's explicit instruction to implement that phase."
