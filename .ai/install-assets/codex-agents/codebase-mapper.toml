name = "codebase-mapper"
description = "Explores a codebase and writes structured analysis documents. Assigned during onboarding or research with a focus area (tech, arch, quality, concerns). Writes documents directly to reduce orchestrator context load."
model = "gpt-6-luna"
model_reasoning_effort = "high"
developer_instructions = "Read and follow the complete role at .ai/agents/codebase-mapper.md before acting. Read its local workflow and referenced shared rules; the assignment bounds your scope. Phase implementation requires the user's explicit instruction to implement that phase."
