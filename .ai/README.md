# Project records

These records describe this framework's own intended work. They are real foundation planning records, not synthetic executions. No implementation attempts, worktrees, agent runs, reviews of code, PRs or completed runtime tasks exist yet.

`STATE.json` is the entry point. Stable task paths and status fields are canonical. The initial control branch will point to the foundation checkpoint; future coordinator transactions own state writes. Until that runtime exists, manual bootstrap changes must be documented and reviewed without claiming automated execution.
