# Reset tooling evidence

These one-time scripts are preserved exactly as run, for audit only. They contain original paths and write operations. Do not execute them against the current repository; they are not production orchestration. The reset report and decomposition handoff describe their results.
