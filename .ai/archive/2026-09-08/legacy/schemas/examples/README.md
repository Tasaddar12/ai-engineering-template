# Synthetic contract fixtures

Every JSON file here is a synthetic shape example used for schema validation. Placeholder IDs, hashes, model names, timestamps and Git OIDs do not represent real executions, approvals or commits. These minimal shape examples are not ready-to-dispatch workflow records; semantic validation is a separate product contract.
