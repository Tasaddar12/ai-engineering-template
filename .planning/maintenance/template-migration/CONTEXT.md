# Template migration

## Authorization

On 2026-09-13 the user authorized importing every complete source template,
reconciling referenced components, moving project management into `.planning`,
expanding unmatched local templates, documenting the complete workflow, frequent
commits and pushes to a draft PR, independent review, merge and safe cleanup.
Do not shorten upstream guidance. Record every adaptation in `changes.log`.
The subsequent broader skills/commands/agents/workflows expansion remains a later
stage; implement the supporting connections needed to make these templates usable now.

## Source

Source identity, exact revision and required license attribution are maintained
in [third-party notices](../../../.ai/library/THIRD-PARTY-NOTICES.md) and the
linked provenance records. Working instructions use our own workflow namespace.

## Acceptance

- All 40 upstream template files are accounted for, complete and traceable.
- Examples, counterexamples, consumer descriptions and referenced methods survive.
- Local reference mappings identify real supporting files and executable boundaries.
- Project identity, requirements, roadmap, state, phases, research, specifications,
  decisions and project execution settings live under `.planning`; `.ai` contains
  reusable instructions, templates and tooling.
- The runtime consumes the adopted artifacts and preserves ownership, concurrency,
  integration, recovery, independent verification and publication safeguards.
- Local templates without upstream equivalents teach purpose, consumers, filling
  instructions, good/bad examples and handoffs at comparable depth.
- Human documentation explains onboarding through delivery with concise tables,
  lists and examples; it distinguishes available behavior from later expansion.
- Real Git/process tests, template provenance/reference checks, hook tests and
  independent review pass before merge. Draft pushes are progress snapshots.

## Slices

1. Record scope and open a draft PR.
2. Import complete pinned templates, supporting sources and provenance.
3. Separate project data and adapt runtime consumption with regression evidence.
4. Reconcile local instructions and expand local-only templates and user guides.
5. Independently review, fix findings, validate, merge and clean up.

## Decisions

- The latest user request supersedes earlier restrictions where necessary to
  preserve the requested upstream templates, including phase-local PLAN filenames
  and optional milestone templates. No standalone work-item lifecycle is added.
- Keep the adopting project's identity unfilled. These maintenance records are
  explicitly authorized by the current request and are not a fictional adoption.
- Preserve the existing uncommitted primary CONTEXT edit. Do not overwrite it.
- Draft publication before completion is explicitly authorized by the user.
  Marking ready and merging still requires final checks and independent review.

## Latest user direction — 2026-09-13

Remove source-project branding from active names, folders and guides. Preserve
required source attribution in third-party/provenance records. Keep complete
instructional content while adapting terminology and references consistently.

The user proposed three future entry workflows: feature changes (possibly grouped
by a feature or milestone spanning phases), bug fixes, and small changes. Start
from the requested outcome, research and informed questions, then establish phase
goals. Record this as the proposed next-stage design; do not claim unimplemented
new dispatch or grouping features.

Independent review findings and their original evidence are in REVIEW-01.md.
Correct required gaps and obtain current re-review before final readiness/merge.
