# Independent integration review — gaps found

Reviewed revision: `f27df19cd732f889cd60509c507a28ab6a936a0d`.
Reviewer: independent read-only review agent, assigned `codex/gsd-review` checkout.
This report preserves findings at that revision; later corrections do not rewrite
the original result.

| Finding | Evidence | Required correction |
|---|---|---|
| Native TDD plan rejected | A real temporary Git fixture with the referenced `<feature>/<behavior>/<implementation>` TDD shape failed readiness with `missing <tasks> instructions` | Accept the full native structure or specify a complete additive adapter without deleting the feature |
| Six real supporting hyperlinks unresolved | Skill bootstrap, loop-hook dispatch (two links), MVP concepts, planning configuration and reviewer instances link to source documents that exist at the pinned revision but not at their relocated relative paths | Repair actual destinations and audit real Markdown links, including fragments |
| Summary variants underspecified | Runtime requires default section names absent/different in standard, minimal and complex variants | Specify complete additive requirements or support evidenced variant structure |
| Windows test decoding | A test read the UTF-8 generated roadmap with default cp1252 and raised UnicodeDecodeError | Use explicit UTF-8 encoding |

## Positive evidence

- Independent regeneration from pinned Git blobs: 491 outputs, zero differences.
- All 40 templates and 447 supporting files retained with reversible adaptations.
- Seven provenance/template tests and active workflow navigation passed.
- Full artifact fixture exercised execution, verification, acceptance, state
  preservation and publication through its simulated external boundary.
- Local-only decision/current-spec templates have purpose, consumers, complete
  artifact blocks, good/bad examples and handoffs.
- Human guides describe actual workflow behavior without claiming the entire
  source runtime is installed.

## Limits and continuation

The broad phase-record suite ran 14 tests in 74.537 seconds. Eight navigation
subtests failed: six actual broken links above plus two illustrative links that
need narrowly scoped example handling. One UTF-8 decoding error also occurred.

Deletion declarations and roadmap protection changes were newer than this
review. Review those alongside the corrections at the final integrated revision.
The user subsequently requested neutral workflow naming and proposed three
entry paths; verify resulting names/references without shortening instructions.
