# Disposable integration pilot

This checkout is an authorized orchestration integration fixture. Workers implement only their assigned PLAN's acceptance in the declared owned paths. Read .ai/RULES.md. The coordinator owns commits, staging, PRs, merges and lifecycle records. Workers must not stage, commit, push, create records, launch agents or touch other worktrees. No additional plans, specs, documentation, decisions or journals are needed. Keep changes small and return the assigned JSON schema. Review source and tests against the assigned acceptance, without expanding scope to template placeholders.
